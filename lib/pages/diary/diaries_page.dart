import 'package:flutter/material.dart';

class DiariesPage extends StatelessWidget {
  const DiariesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is the Diaries Page'));
  }
}
